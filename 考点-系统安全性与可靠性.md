## 安全性与可靠性知识架构

SECURITY & RELIABILITY
```
├── 安全性架构 (防破坏) ─── [1. 加密解密] [2. 身份认证] [3. 访问控制]
└── 可靠性架构 (抗故障) ─── [4. 容灾备份] [5. 冗余设计] [6. 软件容错]
```

------------------------------
## 🛡️ 第一部分：系统安全性架构（安防三道防线）
安全性（Security）的核心目标是保证数据的机密性、完整性和可用性。在架构设计中，通常通过以下三道防线来实现。
## 1. 第一道防线：加密解密机制（数据看不懂）
在软考中，加密机制的选型主要考查对称加密、非对称加密与摘要算法的混合使用。

* 对称加密（Symmetric Cryptography）：
* 核心特点：加密和解密用同一种密钥。速度极快，适合加密大文件或海量数据。
   * 常用算法：AES（最常用）、DES、3DES。
   * 致命缺点：密钥分发困难，不适合在互联网上直接传输密钥。
* 非对称加密（Asymmetric Cryptography）：
* 核心特点：有公钥（公开）和私钥（保密）。公钥加密只能私钥解，私钥加密只能公钥解。
   * 常用算法：RSA、ECC。
   * 致命缺点：计算极其复杂，速度慢，不适合加密大文件。
* 摘要算法（Digital Digest / 散列算法）：
* 核心特点：单向不可逆。任意长度输入得到固定长度输出，只要内容变动，摘要必定改变。
   * 常用算法：SHA-256、MD5（已不安全，少用）。
   * 选型场景：配合非对称加密形成数字签名（防止数据被篡改、防否认），或用于数据库用户密码存储。

## 2. 第二道防线：身份认证（进门验身份）
确定“你是谁”的过程，防止非法用户冒充。

* 双因子认证（2FA/MFA）：结合“你知道的（密码）”和“你拥有的（手机验证码、动态令牌）”或“你的生物特征（指纹、面部）”。（案例分析中提升系统安全性的标准建议）
* 数字证书与 CA 机构：利用第三方权威机构（CA）颁发的证书，验证公开密钥的真实性，防止中间人攻击（如 HTTPS）。

## 3. 第三道防线：访问控制（进门后能干啥）
确定“你能看什么、能改什么”的过程。软考中最核心的两种模型：

* 自主访问控制（DAC）：资源的拥有者可以直接授权其他人访问该资源（如 Linux 的 chmod）。灵活性高，但安全性低。
* 基于角色的访问控制（RBAC - 必背考点）：
* 核心逻辑：用户 ➡️ 角色 ➡️ 权限。用户不直接关联权限，而是通过赋予“角色”（如管理员、财务、普通员工）来间接获得权限。
   * 架构优势：极大地简化了权限管理，降低了配置出错的概率，易于维护。

------------------------------
## ⚙️ 第二部分：系统可靠性架构（容错与抗灾）
可靠性（Reliability）的核心目标是系统在发生物理故障、软件 Bug 或天灾时，依然能够正确提供服务。
## 4. 冗余设计（硬件级抗灾）
利用增加多余的资源（硬件、时间、数据）来换取高可用性。

* 结构冗余：
* 冷备份（Cold Standby）：备用机平时处于关机或不工作状态，主机挂了，备用机人工或脚本点火启动。（缺点：恢复时间长，数据有丢失风险）
   * 热备份（Hot Standby / 容灾双活）：备用机与主机同时运行，数据实时同步。主机挂了，系统自动实现秒级无缝切换。
* 信息冗余：如奇偶校验码、海明码、RAID 磁盘阵列（通过增加冗余数据来纠错）。

## 5. 软件容错技术（软件级防 Bug）
当软件内部由于编码错误或运行环境导致异常时，系统不崩溃的防御技术。软考案例分析最喜欢考“恢复块”与“N版本”的对比。

* N版本程序设计（N-Version Programming）：
* 工作原理：由多个独立的团队，使用不同的设计方法、语言，开发出 N 个功能完全相同的软件版本（N $\ge$ 2）。系统运行时，N 个版本同时并行计算，然后通过一个表决器（Voter）对结果进行投票，少数服从多数，输出正确结果。
   * 核心关键字：静态容错、并行运行、多数表决。
* 恢复块方法（Recovery Block）：
* 工作原理：系统设计一个主块（Primary Block）和若干个备用块（Alternating Blocks）。运行时先跑主块，结束后进行验收测试（Acceptance Test）。如果通过，输出结果；如果失败，则状态回滚，执行第一个备用块，再次测试。以此类推，直到成功或全部失败。
   * 核心关键字：动态容错、串行试错、验收测试、状态回滚。

💡 对比记忆法：
N版本是“齐头并进，举手表决”（费硬件、费时间、适合高并发/强实时，如航天器控制）。
恢复块是“不行再试，直到成功”（省硬件、费时间、适合对时间不敏感但注重结果正确的场景）。

## 6. 容灾备份（天灾级防御）
防范地震、火灾、断电等导致整个数据中心毁灭的灾难。

* 两地三中心架构（必背专有名词）：
* 同城双活（或主备）中心：本地部署两个数据中心，光纤直连，数据同步复制，实现零数据丢失（RPO=0），低延迟切换。
   * 异地灾备中心：在遥远的另一个城市部署一个灾备中心，数据异步复制。防止发生区域性自然灾害（如地震）时全军覆没。

------------------------------
## 考试答题万能话术记忆（安全与可靠性小题直用）

* 题目问：“系统要在互联网上传输核心机密数据，如何保证数据安全和防篡改？”

答：“应当采用混合加密架构。利用 AES 对称加密算法 对核心数据进行高效加密；利用 RSA 非对称加密算法 传输 AES 的密钥；同时，利用 SHA-256 算法 生成数据摘要，并结合私钥生成数字签名，以确保数据的机密性、完整性和防否认性。”

* 题目问：“该航空/金融核心控制模块对可靠性要求极高，不允许任何单点故障，应采用何种软件和硬件架构？”

答：“硬件上应当采用集群部署的热备份（或多活）冗余设计，实现故障秒级无缝切换；软件上应当引入N版本程序设计技术，由不同团队开发多个独立版本并行运行，通过表决器对输出结果进行多数表决，实现静态容错，确保系统的高可靠性。”
