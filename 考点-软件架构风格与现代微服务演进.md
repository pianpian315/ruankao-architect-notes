## 第一板块：经典架构风格（上午选择题核心字典）
只需记住“高频词”与风格的对应关系，选择题即可秒杀：

| 架构风格 | 核心特征 | 典型代表 | 核心优点 | 核心缺点 | 考点陷阱 |
|---|---|---|---|---|---|
| 数据流风格 | 强调数据处理的顺序与转换 | 管道-过滤器 | 高复用性、可维护性 | 不适合交互式系统 | 过滤器之间独立，不共享状态 |
| 调用/返回风格 | 组件之间存在明确的调用控制关系 | 主程序/子程序、面向对象 | 结构清晰、易于理解 | 改变接口影响全局 | 容易产生紧耦合 |
| 独立构件风格 | 构件间通过触发事件进行异步通信 | 事件驱动系统 | 松耦合、高扩展性 | 缺少全局控制、数据一致性难保证 | 组件不知道谁会接收事件 |
| 虚拟机风格 | 模拟硬件执行特定的自定义语言 | 解释器、规则系统 | 灵活性高、可定制规则 | 运行效率低 | 适用于业务规则频繁变动的场景 |
| 仓库/数据导向风格 | 围绕中央数据结构进行交互 | 数据库系统、黑板系统 | 数据共享高效 | 中央数据中心是瓶颈 | 黑板系统用于知识求解与不确定性问题 |

------------------------------
## 第二板块：从单体到微服务的演进对比（下午填空题/连线题）
用 3 个一句话 记住演进本质：

   1. 单体架构 $\rightarrow$ 聪明端点，没有管线（逻辑全揉在一个大包里）。
   2. SOA 架构 $\rightarrow$ 聪明管线，愚笨端点（逻辑和转换全卡在 ESB 总线里）。
   3. 微服务架构 $\rightarrow$ 愚笨管线，聪明端点（总线退化为轻量级 HTTP/gRPC，逻辑回流到每个微服务内部）。

## 必考对比矩阵：

* 服务粒度：SOA 粗（按业务大功能） $\rightarrow$ 微服务 细（按 DDD 限界上下文）。
* 数据管理：SOA 中央共享数据库 $\rightarrow$ 微服务 去中心化（独占数据库/Database per Service）。
* 通信协议：SOA 笨重协议（SOAP/WSDL）+ ESB $\rightarrow$ 微服务 轻量级（REST API / gRPC）。

------------------------------
## 第三板块：微服务核心“四梁八柱”（案例/论文万能架构图组件）
在答题或画图时，现代微服务系统必须包含以下 5 个核心组件：
```python
                    ┌────────────────────────────────────────┐
                    │            API 网关 (统一入口)          │
                    └───────────────────┬────────────────────┘
                                        │ (路由/路由限流)
                    ┌───────────────────▼────────────────────┐
                    │         服务注册中心 (Nacos / Consul)   │
                    └───────────────────┬────────────────────┘
                                        │ (动态发现)
         ┌──────────────────────────────┼──────────────────────────────┐
         ▼                              ▼                              ▼
┌──────────────────┐           ┌──────────────────┐           ┌──────────────────┐
│   微服务 A (写)   │           │   微服务 B (读)   │           │  微服务 C (其他)  │
│ ┌──────────────┐ │           │ ┌──────────────┐ │           │ ┌──────────────┐ │
│ │ 独占数据库A  │ │           │ │ 独占数据库B  │ │           │ │  断路器组件  │ │
│ └──────┬───────┘ │           │ └──────────────┘ │           │ └──────────────┘ │
└────────│─────────┘           └──────────────────┘           └──────────────────┘
         │ (发布事件)
         ▼
┌────────────────────────────────────────────────────────────────────────────────┐
│                           消息队列 (MQ / 事件驱动异步解耦)                       │
└────────────────────────────────────────────────────────────────────────────────┘
```

   1. API 网关 (API Gateway)：系统的唯一入口。负责路由、鉴权、限流、日志。
   2. 服务注册与发现 (Discovery)：服务节点的动态大管家。解决实例动态上下线后的路由问题。
   3. 断路器 (Circuit Breaker)：防雪崩利器。具备 Closed（正常） $\rightarrow$ Open（熔断降级） $\rightarrow$ Half-Open（尝试恢复） 三种状态。
   4. 配置中心 (Config)：外部化配置。实现“修改配置，实时生效，无需重启”。
   5. 消息队列 (MQ)：实现服务间的异步事件驱动风格，完成系统彻底解耦。

------------------------------
## 第四板块：微服务带来的“后遗症”与药方（下午大题必背简答话术）
微服务不是银弹，引入它会带来分布式副作用。真题极爱考查“遇到了XX问题，架构师该怎么解决”：
## 问题 1：分布式事务（数据一致性）难题

* 原因：数据库被拆分（Database per Service），ACID 强一致性失效。
* 药方（答题词）：放弃强一致性，追求最终一致性（BASE理论）。采用 Saga 模式（正向补偿/逆向回滚）或 消息队列（MQ）可靠消息最终一致性方案。

## 问题 2：跨服务复杂关联查询难题

* 原因：严禁跨服务 Join 数据库，导致原先一条 SQL 能解决的报表现在无法直接查询。
* 药方（答题词）：采用 CQRS（读写职责分离）模式。写服务通过 MQ 发送状态变更事件，读服务订阅事件并在自己的数据库中维护一张面向查询的聚合宽表。

## 问题 3：服务雪崩（级联故障）

* 原因：服务链路太长（A 调 B，B 调 C），C 死机导致 B 阻塞，最终导致整个系统瘫痪。
* 药方（答题词）：引入断路器模式。设置合理的超时时间，一旦失败率达阈值自动熔断，并提供降级（Fallback）托底数据。

------------------------------
## 💡 复习通关建议

   1. 选择题（上午）：重点看第一板块的“高频词”和第二板块的对比，看到关键字直接选。
   2. 案例题（下午I）：重点背诵第四板块的三个“问题与药方”。原题通常是“该系统重构后遇到了分布式事务和雪崩问题，请给出解决方案”，直接默写药方即可拿满分。
   3. 论文题（下午II）：熟练画出第三板块的架构组件关系。在文章中按照“原单体分层风格的痛点 $\rightarrow$ 使用 DDD 拆分为微服务 $\rightarrow$ 引入网关/MQ/断路器解决分布式难题”的逻辑来写。


