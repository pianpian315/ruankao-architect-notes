## 数据库系统设计核心知识体系

DATABASE DESIGN ARCHITECTURE
1. 伸缩性架构 (主从复制、读写分离)
2. 分布式扩展 (分库分表策略)
3. 混合存储选型 (SQL vs NoSQL)
4. 性能催化剂 (Redis 缓存架构)

------------------------------
## 一、 伸缩性架构：主从复制与读写分离
这是应对“读多写少”高并发场景的基石架构。
## 1. 主从复制（Master-Slave Replication）

* 核心原理：主库（Master）记录所有写操作到二进制日志（如 MySQL 的 Binlog）；从库（Slave）通过 I/O 线程读取该日志，并由 SQL 线程在本地重放（Relay Log），实现数据同步。
* 同步机制对比（软考极易考选择/简答）：
* 同步复制：主库等所有从库都写成功才返回。安全，但性能极差。
   * 异步复制：主库写成功立即返回。性能高，但主库宕机可能丢失数据。
   * 半同步复制：主库等至少一个从库收到日志（无需写入完成）就返回。性能与安全的折中方案。

## 2. 读写分离（Read/Write Splitting）

* 核心做法：写操作全部路由到主库，读操作分摊到各个从库。
* 典型痛点：主从复制延迟
* 现象：用户刚发布（写主库），立即刷新页面（读从库），发现数据没出来。
   * 通用架构解决方案：
   1. 关键业务强制读主库（如：涉及钱、订单状态、刚提交的表单）。
      2. 延迟估算与路由：估算复制延迟，若延迟超标，路由回主库。
      3. 缓存过渡法：写入主库的同时写入 Redis，前端优先读 Redis。
   
------------------------------
## 二、 分布式扩展：分库分表策略
当单表数据量超千万或单库并发达到瓶颈时，必须进行数据分片。
## 1. 切分维度

* 垂直切分（按业务拆）：
* 垂直分库：将电商系统中的 用户表、商品表、订单表 分别放到不同的数据库实例中。
   * 垂直分表：将一张表中冷热字段分离。如把大字段（商品详情 text）和高频访问字段（价格、库存）拆成两张表。
* 水平切分（按数据量拆）：
* 保持表结构不变，将行数据按某种规则分发到不同的库或表中。

## 2. 水平分片策略及优缺点

* 哈希取模（Hash）：按 ID % 节点数 分片。
* 优点：数据分布极其均匀，并发读写分散。
   * 缺点：扩容极其麻烦，需要面临整体数据迁移。
* 范围分片（Range）：按时间（如每月一张表）或自增 ID（每 1000 万一条表）。
* 优点：扩容简单，不涉及旧数据迁移。
   * 缺点：容易产生数据尾部热点（最新的表并发最高）。

## 3. 分库分表引入的新问题（案例分析的高频痛点分析题）

* 分布式事务：跨库写操作无法使用本地事务（解决方案：二阶段提交 2PC、TCC、MQ 最终一致性）。
* 跨节点 Join 困难：无法直接连接查询（解决方案：全局表冗余、业务层代码组装、ETL 抽取到数仓）。
* 分布式全局唯一 ID：自增 ID 冲突（解决方案：雪花算法 Snowflake、UUID、Redis 自增）。

------------------------------
## 三、 混合存储选型：关系型（SQL）vs 非关系型（NoSQL）
在架构设计中，通常采用 Polyglot Persistence（混合持久化） 架构。
## 1. 核心特征对比表

| 特征 | 关系型数据库 (SQL, 如 MySQL) | 非关系型数据库 (NoSQL, 如 MongoDB/Redis) |
|---|---|---|
| 数据模型 | 严谨的二维表格，预定义 Schema | 键值对、文档、列族、图形 |
| 事务支持 | 强事务（严格遵循 ACID 理论） | 弱事务（通常遵循 BASE 理论，追求最终一致性） |
| 扩展方式 | 纵向扩展（Scale-up，提升单机硬件） | 横向扩展（Scale-out，加机器，天然分布式） |

## 2. NoSQL 经典四大分类与软考选型场景

   1. 键值（Key-Value）：如 Redis。
   * 选型场景：高频访问的热点数据缓存、分布式锁、计数器。
   2. 文档型（Document）：如 MongoDB。
   * 选型场景：日志系统、商品详情页（属性经常变动）、数据结构不固定的业务。
   3. 列族（Column-Oriented）：如 HBase。
   * 选型场景：海量历史数据存储（上亿级别）、大数据实时读写、物联网传感器数据。
   4. 图数据库（Graph）：如 Neo4j。
   * 选型场景：社交网络关系链、风控知识图谱、推荐系统。
   
------------------------------
## 四、 性能催化剂：Redis 缓存架构与三类特异风险
缓存是系统架构中提升性能（Performance）最立竿见影的技术，但引入后会带来复杂性。
## 1. 缓存三类特异风险（案例分析必考，请死记硬背特征与解法）## ① 缓存穿透（穿过缓存，直击 DB，但数据不存在）

* 特征描述：查询一个既不在缓存中，也不在数据库中的数据（如黑客恶意构造的非法 ID）。导致每次请求都穿透到数据库，导致数据库瞬间崩溃。
* 标准架构解法：
1. 布隆过滤器（Bloom Filter）：在访问缓存前加一层布隆过滤器，快速拦截不存在的非法请求。
   2. 缓存空对象（Cache Null）：如果 DB 返回空，也向缓存写入一个 NULL 或特定标记，并设置极短的过期时间（如 60 秒）。

## ② 缓存雪崩（大面积缓存同时失效）

* 特征描述：大量热点数据具有相同的过期时间，或者 Redis 缓存节点突然宕机，导致某一时刻大面积请求全部涌入数据库。
* 标准架构解法：
1. 过期时间加随机扰动：设置过期时间时，加上一个随机值（如 1-5 分钟），防止数据集中失效。
   2. 高可用集群架构：部署 Redis Sentinel（哨兵模式）或 Redis Cluster（集群模式），避免单点故障。
   3. 多级缓存：引入本地缓存（如 Caffeine），作为 Redis 的备用防线。

## ③ 缓存击穿（单点爆破，热点数据失效）

* 特征描述：某一个超级热门的数据（如微博热搜），在过期的瞬间，有数十万并发请求同时涌入，由于缓存没有，所有请求同时去数据库查询。
* 标准架构解法：
1. 热点数据永不过期：后台启动异步线程，定时给热点数据“续期”。
   2. 互斥锁（Mutex Lock）：在缓存失效时，只允许一个请求持有分布式锁去查 DB 并回写缓存，其他请求等待重试。

## 2. 双写一致性策略（DB 与 Redis 的同步）
高并发下，同时更新数据库和缓存容易产生脏数据。软考中最认可的标准考场架构方案有两类：

* 方案 A：Cache Aside 模式（先更新 DB，再删除缓存）
* 为什么是删除而不是更新？ 因为更新缓存开销大，且容易引发并发覆盖。
   * 极端并发漏洞：读写并发时仍有极小概率产生脏数据。
   * 终极补救：延迟双删（写入 DB 后，先删一次缓存，睡眠 1-2 秒，再删一次）。
* 方案 B：异步可靠通知模式（基于 Binlog 同步）
* 做法：应用只负责写数据库。利用 Canal 等中间件订阅数据库的 Binlog 日志，解析后异步发送到消息队列（MQ），由专门的消费者去更新/删除 Redis 缓存。
   * 架构优势：将业务代码与缓存解耦，保证可靠性和最终一致性。

------------------------------
## 考试答题万能话术记忆（案例小题直用）

* 题目问“如何提升现有关系型数据库的并发读取能力”：

答：“可以引入 Redis 缓存层 存储热点数据，并在底层实施 读写分离与主从复制 架构，将读流量分摊到多个从库，从而提升系统的整体并发和吞吐量。”

* 题目问“随着业务发展，单表数据量过大导致查询缓慢，如何优化”：

答：“应当采用 水平分库分表 策略。根据核心业务主键（如用户 ID 或订单 ID）作为分片键进行 哈希取模或范围分片，将数据和流量打散到分布式节点中。”
